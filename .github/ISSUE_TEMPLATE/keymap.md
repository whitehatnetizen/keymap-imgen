---
name: A keymap fails or draws nothing
about: The command stops with a message, or the output is empty or broken
title: "Keymap: "
labels: keymap
---

**The command you ran** and what it printed:

```
python keymap-imgen.py ...
```

**The keymap file** (`.vil` or `keymap.json`), attached if you can. A `.vil` holds keycodes,
layer names and the board's uid, nothing personal.

**Your system** (Windows, macOS or Linux, and the Python version from `python --version`):
